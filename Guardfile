# Reloads the test results in chrome when they change. See
# https://github.com/guard/guard-livereload for installation and running
guard 'livereload' do
  watch(%r{build/reports/tests/Gradle suite/Gradle test\.html})
end
